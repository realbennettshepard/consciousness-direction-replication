__pycache__/
*.pyc

# superseded direction files — directions_llama8b_fixed.npz is the current one
directions_Qwen2.5-1.5B-Instruct.npz
directions_Meta-Llama-3-8B-Instruct.npz
directions_Meta-Llama-3-8B-Instruct-8bit.npz

# harmful-prompt corpora for the safety-ablation arm are public elsewhere; not
# redistributed here (see README)
harmful_prompts*.json
advbench*.json
jbb*.json

# pre-rerun snapshots kept locally for diffing; the committed JSONs are current
old_*.json

# safety-ablation arm: never commit harmful prompts, model completions, or the
# refusal-direction artifacts derived from them. Only aggregate rates (committed
# explicitly) leave this machine.
directions_refusal.npz
refusal_selected.json
*_completions*.json
*_generations*.json
